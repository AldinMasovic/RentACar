<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentACar"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CarDetailsActivity -->
        <activity
            android:name=".CarDetailsActivity"
            android:label="Car Details Activity">
        </activity>

        <!-- UserProfileActivity -->
        <activity
            android:name=".ProfileActivity"
            android:label="User Profile Activity">
        </activity>

        <!-- SignInActivity -->
        <activity
            android:name=".SignInActivity"
            android:label="Sign in Activity">
        </activity>

        <!-- HomeActivity -->
        <activity
            android:name=".HomeActivity"
            android:label="Home Activity">
        </activity>

        <!-- SearchResultsActivity -->
        <activity
            android:name=".SearchResultsActivity"
            android:label="Search Results Activity">
        </activity>

        <!-- SignUpActivity -->
        <activity
            android:name=".SignUpActivity"
            android:label="SignUpActivity Activity">
        </activity>

        <!-- ReservationsActivity -->
        <activity
            android:name=".ReservationsActivity"
            android:label="Reservations Activity">
        </activity>


    </application>

</manifest>